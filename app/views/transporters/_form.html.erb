<%= form_with(model: transporter, local: true) do |form| %>
  <% if transporter.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(transporter.errors.count, "error") %> prohibited this transporter from being saved:</h2>

      <ul>
      <% transporter.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :transporter_name %>
    <%= form.text_field :transporter_name %>
  </div>

  <div class="field">
    <%= form.label :address %>
    <%= form.text_field :address %>
  </div>

  <div class="field">
    <%= form.label :phone %>
    <%= form.text_field :phone %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
